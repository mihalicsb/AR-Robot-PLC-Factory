@page
@model ManagementWebServer.Pages.robot.robottpye.EditModel

@{
    ViewData["Title"] = "Robot típus módosítása";
}

<h4>@ViewData["Title"]</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RobotType.Id" />
            <div class="form-group">
                <label asp-for="RobotType.Name" class="control-label"></label>
                <input asp-for="RobotType.Name" class="form-control" />
                <span asp-validation-for="RobotType.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <br/>
                <input type="submit" value="Mentés" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-secondary" asp-page="./Index">Mégsem</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
